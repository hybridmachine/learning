clc; disp (['Welcome to MATLAB, the current working directory is ' pwd()]);
disp(['May the source be with you']);
beep off;
set(0,'DefaultFigureWindowStyle','docked');
set(0,'DefaultFigureColorMap',jet), close all;